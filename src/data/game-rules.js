import { TOTAL_BOSSES } from './hangar.js';

export const RUN_RULES = Object.freeze({
  bossCount: TOTAL_BOSSES,
  waveLength: 21,
  waveDeadline: 31,
  waveBreak: 2.1,
  enemyCap: 86,
  enemySpawnScale: .9,
});
